#include "main.h"
/**
* _strcpy - prints a number given of elements
* @dest : pointer dest
* @src : pointer src
* Return: Return dest
*/
char *_strcpy(char *dest, char *src)
{
int check, x;
for (check = 0; src[check] != '\0'; check++)
{
}
for (x = 0; x <= check; x++)
{
dest[x] = src[x];
}
return (dest);
}
